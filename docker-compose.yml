version: '3.8'

services:
  api:
    build: .
    container_name: springboot-api
    ports:
      - "8080:8080"  # Expone el puerto 8080 del contenedor al puerto 8080 de la máquina host (VPS)
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
