version: '3.8'

services:
  api:
    build: .
    container_name: springboot-api
    ports:
      - "5000:8080"  # Aquí exponemos el puerto 8080 del contenedor como 5000 en el host
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
