services:
  web:
    image: nginx:latest
    volumes:
      - ./path/to/your/web:/usr/share/nginx/html
    ports:
      - "80:80"
  api:
    image: your-api-image
    ports:
      - "5000:5000"
    environment:
      - API_ENV=production
